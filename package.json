{
  "name": "gits-ui",
  "version": "0.1.0",
  "description": "Ghost in the Shell aesthetic web UI components",
  "type": "module",
  "scripts": {
    "start": "vite --open /src/index.html",
    "build": "vite build",
    "deploy": "vite build && cp dist/src/index.html . && rm -f assets/*.js assets/*.css assets/*.ttf assets/*.jpg && cp dist/assets/* assets/ && cp dist/sonar-ping.mp3 . 2>/dev/null; cp -r dist/data . 2>/dev/null; true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "world-atlas": "^2.0.2"
  },
  "dependencies": {
    "postprocessing": "^6.38.3",
    "three": "^0.183.2",
    "topojson-client": "^3.1.0"
  }
}
