{
  "name": "gits-ui",
  "version": "0.1.0",
  "description": "Ghost in the Shell aesthetic web UI components",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build && cp dist/assets/* assets/",
    "deploy": "vite build && cp dist/index.html . && cp dist/assets/* assets/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "world-atlas": "^2.0.2"
  },
  "dependencies": {
    "three": "^0.183.2",
    "topojson-client": "^3.1.0"
  }
}
